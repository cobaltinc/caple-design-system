import React from 'react';
import {
  Grid
} from '../src';

export default {
  title: 'Grid',
};

export const Default = () => {
  return (
    <div style={{padding: 30}}>
      WIP
    </div>
  )
};